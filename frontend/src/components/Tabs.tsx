import React from 'react';type T={tabs:{key:string;label:string}[],active:string,onChange:(k:string)=>void};export default function Tabs({tabs,active,onChange}:T){return <div style={{display:'flex',gap:8,marginBottom:12}}>{tabs.map(t=><button key={t.key} onClick={()=>onChange(t.key)} className='btn' style={{background:active===t.key?'var(--fg)':'var(--card)',color:active===t.key?'var(--bg)':'var(--fg)'}}>{t.label}</button>)}</div>;}